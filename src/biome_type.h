#pragma once

enum Biome_Type {
  ARCTIC = 0,
  DESERT = 1,
  FOREST = 2,
  MOUNTAIN = 3,
  OCEAN = 4 // for now water will probably just be a blue tinging shader affect
            // and not actually rendered blocks
};